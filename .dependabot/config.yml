# This is an example with only required properties:
version: 1
update_configs:
  - package_manager: "php"
    directory: "/server"
    update_schedule: "live"
    default_labels:
      - "dependencies"
      - "dependabot"
      - "update"
